{
  "$schema": "./schema.json",
  "migrations": [
    {
      "id": "theme-editor-enqueue-iframe",
      "title": "Move editor canvas styles to enqueue_block_assets (WP 7.0 iframe editor)",
      "description": "Rewrites the theme app/setup.php so editor.css + button.style.css move from enqueue_block_editor_assets to a new enqueue_block_assets hook (is_admin guard); the editor JS stays put.",
      "appliesTo": {
        "package": "webentor-core",
        "from": "0.12.0",
        "to": "0.13.0"
      },
      "rules": ["rules/editor-enqueue-iframe.yml"],
      "docs": "README.md"
    }
  ]
}
