# Concerns

> Auto-generated by gsd-codebase-mapper (concerns focus)

## Technical Debt

### TODO Items in Source Code

There are ~17 TODO/FIXME comments scattered across the codebase:

**Core blocks:**
- `packages/webentor-core/resources/blocks/l-nav-menu/l-nav-menu.block.tsx:43` — "check if works with Polylang"
- `packages/webentor-core/resources/blocks/e-table-row/e-table-row.block.tsx:103` — "extract icon to icon set"
- `packages/webentor-core/resources/blocks/e-table-cell/e-table-cell.block.tsx:168` — "extract icon to icon set" (duplicate pattern)
- `packages/webentor-core/resources/blocks/e-slider/e-slider.block.tsx:26` — "extend with slider/responsive settings"
- `packages/webentor-core/resources/blocks/l-site-logo/l-site-logo.block.tsx:6` — "Add logo image?"
- `packages/webentor-core/resources/blocks/e-picker-query-loop/e-picker-query-loop.block.tsx:103` — "Maybe rework to nicer UI"
- `packages/webentor-core/resources/blocks/e-query-loop/e-query-loop.block.tsx:99` — "add settings for these"
- `packages/webentor-core/resources/blocks/e-image/e-image.block.tsx:179` — "maybe move to BlockControls toolbar"
- `packages/webentor-core/resources/blocks/l-nav-menu/style.css:1` — "maybe adjust nested CSS to be compatible with native nesting"

**Core infrastructure:**
- `packages/webentor-core/resources/scripts/editor.ts:4` — "maybe add filter to customize this list"
- `packages/webentor-core/resources/styles/components/_table.css:2` — "adjust variables"
- `packages/webentor-core/resources/views/partials/footer.blade.php:1` — "add basic footer"
- `packages/webentor-core/resources/views/partials/header.blade.php:1` — "add basic header"

**Starter:**
- `packages/webentor-starter/LocalValetDriver.php:13` — "Get from .env file?"
- `packages/webentor-starter/web/app/mu-plugins/webikon-wp-cleanup.php:15` — "add some hooks to enable/disable this"
- `packages/webentor-starter/web/app/themes/webentor-theme-v2/resources/views/ui-kit/ui-kit-core-components.blade.php:8` — "add slider"

### Incomplete Core Components

- **Header and Footer partials** (`packages/webentor-core/resources/views/partials/`) are essentially empty — they contain only TODO comments. These are core framework views that consumer themes are expected to build upon, but the defaults provide no fallback.
- **Site logo block** (`l-site-logo`) has a TODO about missing logo image functionality.

### Icon Extraction Pattern

Two blocks (`e-table-row`, `e-table-cell`) have inline icon SVGs that should be extracted to a shared icon set. This is a recurring pattern that suggests a missing icon management system.

## No Test Coverage

The project has **zero automated tests** (see `TESTING.md`). The CI `pnpm test` commands pass silently due to `--if-present` flags. This means:
- Regressions in the responsive settings system (the most complex subsystem) are undetectable
- Block migration logic (`blocks-migration.php`, `migration.ts`) has no safety net
- PHP rendering and Blade templates are only validated by manual testing

## Responsive Settings Complexity

The responsive settings system at `packages/webentor-core/core-js/blocks-filters/responsive-settings/` is the largest and most complex subsystem:
- Registry-based architecture with ~10 individual setting modules
- Each setting has: registration, properties, settings UI, and panel components
- Generates Tailwind CSS classes across breakpoints
- Has its own migration system
- No unit tests covering any of this

The complexity-to-coverage ratio makes this the highest-risk area for regressions.

## Build & Tooling

### Dormant Playwright Setup
- `playwright-report/` directory exists at root but no Playwright config or tests are present
- Suggests E2E testing was explored but abandoned

### CI Test Gaps
- `pnpm test` is called in CI but silently no-ops (no test scripts defined)
- PHP code has no static analysis (no PHPStan/Psalm configured)
- No PHP code style enforcement (no PHP-CS-Fixer/Pint)

## Security Considerations

### WordPress-Specific
- `packages/webentor-starter/web/app/mu-plugins/webikon-wp-cleanup.php` — WP cleanup plugin lacks hooks to toggle features on/off (noted in its own TODO)
- Block rendering relies on server-side Blade templates with ACF data — standard WordPress escaping practices should be audited for XSS

### Dependency Management
- Large `node_modules` footprint (pnpm store) — standard for the ecosystem but worth monitoring for supply chain risks
- Composer dependencies for WordPress (Bedrock) follow standard security practices

## Performance

### Block Editor Load
- 27 core blocks + theme blocks all register in the editor
- The responsive settings filter attaches to every block — potential editor performance impact at scale
- No evidence of lazy loading or code splitting for block editor scripts

### Frontend
- Alpine.js used for frontend interactivity (lightweight)
- Slider library loaded via `_slider.ts`
- Vite handles bundling and tree-shaking for production builds

## Fragile Areas

1. **Block migration** (`packages/webentor-core/app/blocks-migration.php` + `core-js/blocks-filters/responsive-settings/migration.ts`) — data format changes without tests
2. **Responsive settings registry** — adding new settings requires changes across multiple files (registration, properties, settings UI, panel) with no automated verification
3. **Config schema validation** — schemas exist in `packages/webentor-core/schemas/` but unclear how strictly they're enforced at runtime
4. **Cross-package dependencies** — core and starter are tightly coupled through block definitions, Blade views, and config — changes in core can silently break starter
