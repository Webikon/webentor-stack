# Testing

> Auto-generated by gsd-codebase-mapper (quality focus)

## Current State

**No formal test framework is configured.** The project has no unit tests, integration tests, or E2E test suite in source code. All `*.test.*` and `*.spec.*` files found are in `node_modules/` from dependencies.

## Quality Gates

The project relies on static analysis and linting as primary quality gates:

### Linting

- **ESLint** — configured via flat config in `packages/webentor-core/eslint.config.js` and `packages/webentor-starter/web/app/themes/webentor-theme-v2/eslint.config.js`, with shared config from `packages/webentor-configs/eslint.config.js`
- **Stylelint** — configured in `packages/webentor-core/.stylelintrc.js` and `packages/webentor-starter/web/app/themes/webentor-theme-v2/.stylelintrc.js`
- **Prettier** — configured in `packages/webentor-core/.prettierrc.js` and `packages/webentor-starter/web/app/themes/webentor-theme-v2/.prettierrc.js`

### Type Checking

- **TypeScript** — used across `core-js/` for block editor code (React/TSX)
- Type definitions in `packages/webentor-core/core-js/types/`

### Build Validation

- Vite build for frontend assets (core and theme)
- PHP syntax validation for setup scripts (`php -l`)
- Shell script syntax validation for setup (`bash -n`)

## CI Pipeline

CI is defined in `.github/workflows/ci.yml` with **path-filtered jobs**:

| Job | Triggers on | Checks |
|-----|-------------|--------|
| `core` | `packages/webentor-core/**` | `pnpm lint`, `pnpm build`, `pnpm test` |
| `configs` | `packages/webentor-configs/**` | `pnpm lint`, `pnpm build`, `pnpm test` |
| `setup` | `packages/webentor-setup/**` | `bash -n` (shell syntax), `php -l` (PHP syntax) |
| `starter` | `packages/webentor-starter/**` | `composer install`, `pnpm build` |
| `docs` | `docs/**` | `pnpm docs:build` |

Note: The `pnpm test` commands in CI use `--if-present`, meaning they silently pass when no test script exists.

### Root Scripts

From `package.json`:
- `pnpm lint` — runs lint across all packages
- `pnpm test` — runs test across all packages (currently no-op)

## Pre-Commit Hooks

- **Husky** configured at root level (`.husky/`) and in `packages/webentor-core/.husky/`
- Likely runs linting on staged files before commit

## Playwright (Dormant)

A `playwright-report/` directory exists at the repo root, suggesting Playwright was explored at some point but no active Playwright config or test files exist in the source tree.

## Recommendations for Future Testing

### Priority 1: Block Rendering Tests
- Test that PHP Blade templates render correctly with given attributes
- Test block registration and attribute schemas

### Priority 2: Responsive Settings Unit Tests
- The responsive settings system (`core-js/blocks-filters/responsive-settings/`) is the most complex subsystem
- Unit test the registry, migration logic, and Tailwind class generation

### Priority 3: E2E / Visual Regression
- Leverage the existing `playwright-report/` foundation
- Test block editor interactions and frontend rendering
