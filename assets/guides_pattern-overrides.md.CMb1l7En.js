import{_ as t,o as s,c as a,ag as n}from"./chunks/framework.Bo7atV7P.js";const k=JSON.parse('{"title":"Pattern Overrides","description":"","frontmatter":{},"headers":[],"relativePath":"guides/pattern-overrides.md","filePath":"guides/pattern-overrides.md","lastUpdated":1785931923000}'),i={name:"guides/pattern-overrides.md"};function o(r,e,d,l,h,c){return s(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="pattern-overrides" tabindex="-1">Pattern Overrides <a class="header-anchor" href="#pattern-overrides" aria-label="Permalink to &quot;Pattern Overrides&quot;">​</a></h1><p>WordPress 7.0 opened Pattern Overrides to custom blocks. A synced pattern can now be reused across pages while individual instances override selected content — without detaching the pattern, so design changes still propagate from the original.</p><p>Requires WordPress 7.0 or later. On older versions the controls simply don&#39;t appear; nothing breaks.</p><h2 id="which-blocks-support-it" tabindex="-1">Which blocks support it <a class="header-anchor" href="#which-blocks-support-it" aria-label="Permalink to &quot;Which blocks support it&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Block</th><th>Overridable attributes</th></tr></thead><tbody><tr><td><code>e-button</code></td><td><code>button</code></td></tr><tr><td><code>e-accordion</code></td><td><code>title</code></td></tr><tr><td><code>e-tab-container</code></td><td><code>title</code></td></tr><tr><td><code>e-image</code></td><td><code>imgId</code>, <code>link</code></td></tr><tr><td><code>e-svg</code></td><td><code>imgId</code></td></tr><tr><td><code>e-icon-picker</code></td><td><code>icon</code></td></tr><tr><td><code>e-gallery</code></td><td><code>images</code></td></tr></tbody></table><p>Everything else — spacing, layout, responsive settings, query configuration — is deliberately excluded. Those are design decisions that belong to the pattern, not to the instance.</p><p>Container blocks are absent from this table on purpose. Their content lives in inner blocks, which are overridden individually — see <a href="#container-blocks-overriding-inner-content">Container blocks</a>.</p><h2 id="using-it" tabindex="-1">Using it <a class="header-anchor" href="#using-it" aria-label="Permalink to &quot;Using it&quot;">​</a></h2><ol><li>Create a <strong>synced</strong> pattern containing one of the blocks above.</li><li>Select the block, open <strong>Advanced</strong> in the block sidebar, and click <strong>Enable overrides</strong>. Give the block a name — this name is how each instance identifies which block it is overriding, and it shows up in the pattern&#39;s <strong>Overrides</strong> panel.</li><li>Insert the pattern into a page. Blocks with overrides enabled are editable; every other block in the instance is locked.</li><li>The block toolbar gains a <strong>Reset</strong> action to drop an instance&#39;s override and fall back to the pattern value.</li></ol><p>Override values are stored on the pattern instance (the <code>core/block</code> block&#39;s <code>content</code> attribute), never on the pattern itself.</p><h2 id="container-blocks-overriding-inner-content" tabindex="-1">Container blocks: overriding inner content <a class="header-anchor" href="#container-blocks-overriding-inner-content" aria-label="Permalink to &quot;Container blocks: overriding inner content&quot;">​</a></h2><p>Blocks whose content lives in inner blocks — <code>e-accordion</code>, <code>e-tab-container</code>, <code>e-accordion-group</code>, <code>e-tabs</code>, <code>l-section</code>, <code>l-flexible-container</code>, <code>e-table-cell</code> — work differently, and this is the most common source of confusion.</p><p><strong>You do not enable overrides on the container.</strong> You enable it on each block <em>inside</em> it. The container needs no support of its own; only the leaf blocks that actually hold content do. This mirrors core, where you override the paragraph inside a <code>core/group</code>, never the group.</p><p>So for an accordion panel containing a heading, a paragraph and a CTA:</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!-- wp:webentor/e-accordion {&quot;title&quot;:&quot;FAQ&quot;,&quot;metadata&quot;:{&quot;name&quot;:&quot;faq-1-title&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     &quot;bindings&quot;:{&quot;__default&quot;:{&quot;source&quot;:&quot;core/pattern-overrides&quot;}}}} --&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- wp:heading {&quot;metadata&quot;:{&quot;name&quot;:&quot;faq-1-heading&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       &quot;bindings&quot;:{&quot;__default&quot;:{&quot;source&quot;:&quot;core/pattern-overrides&quot;}}}} --&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Heading&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">h2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- /wp:heading --&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- wp:paragraph {&quot;metadata&quot;:{&quot;name&quot;:&quot;faq-1-body&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       &quot;bindings&quot;:{&quot;__default&quot;:{&quot;source&quot;:&quot;core/pattern-overrides&quot;}}}} --&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Body copy&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- /wp:paragraph --&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- wp:webentor/e-button {&quot;metadata&quot;:{&quot;name&quot;:&quot;faq-1-cta&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       &quot;bindings&quot;:{&quot;__default&quot;:{&quot;source&quot;:&quot;core/pattern-overrides&quot;}}}} /--&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- A block with no override stays identical in every instance. --&gt;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  &lt;!-- wp:paragraph --&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Fine print&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!-- /wp:paragraph --&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">&lt;!-- /wp:webentor/e-accordion --&gt;</span></span></code></pre></div><p>In practice you do this through the UI: expand the pattern in the List View, select each inner block, and enable overrides on it. Every named block then shows up in the pattern&#39;s <strong>Overrides</strong> panel and in the instance&#39;s <strong>Content</strong> panel.</p><p>The accordion&#39;s own <code>title</code> is a separate override from its inner blocks — enable both if editors should be able to change the panel label <em>and</em> its content.</p><h3 id="what-cannot-be-overridden-structure" tabindex="-1">What cannot be overridden: structure <a class="header-anchor" href="#what-cannot-be-overridden-structure" aria-label="Permalink to &quot;What cannot be overridden: structure&quot;">​</a></h3><p>Per-instance <strong>structure</strong> is not possible, and this is a WordPress architectural limit rather than something Webentor can add:</p><ul><li><code>core/block</code> has only two attributes, <code>ref</code> and <code>content</code> (a map of attribute overrides). There is nowhere to store a per-instance block tree.</li><li>A pattern instance serializes as a self-closing block with no inner markup: <code>&lt;!-- wp:block {&quot;ref&quot;:123,&quot;content&quot;:{…}} /--&gt;</code>. The inner blocks are always read from the pattern post.</li><li>Accordingly, the editor blocks insertion inside an instance and discards any structural change.</li></ul><p>Concretely: one synced pattern <strong>cannot</strong> show five accordion items on one page and two on another. Content varies per instance; the number, order and type of blocks do not.</p><p>If you need that, pick the option that matches the real requirement:</p><table tabindex="0"><thead><tr><th>Need</th><th>Approach</th></tr></thead><tbody><tr><td>Same items, different copy per page</td><td>Pattern overrides on inner blocks (above)</td></tr><tr><td>A fixed maximum, with some items hidden per page</td><td>Over-provision the pattern and expose a bindable hide flag on the item block. <code>e-button</code> already supports this via <code>button.showButton</code>, since the whole object is bound</td></tr><tr><td>Genuinely different structure per page</td><td>An <strong>unsynced</strong> pattern — full freedom, but design changes no longer propagate</td></tr><tr><td>Fixed structure, freely editable content, no central sync</td><td>A container with <code>templateLock: &#39;contentOnly&#39;</code> in the page itself</td></tr><tr><td>A repeating list of records</td><td>Model it as content: a CPT with ACF fields plus <code>e-query-loop</code>. If the &quot;pattern&quot; is really data, this is the correct architecture and everything above is a workaround</td></tr></tbody></table><h2 id="caveat-object-attributes-are-overridden-as-a-whole" tabindex="-1">Caveat: object attributes are overridden as a whole <a class="header-anchor" href="#caveat-object-attributes-are-overridden-as-a-whole" aria-label="Permalink to &quot;Caveat: object attributes are overridden as a whole&quot;">​</a></h2><p>WordPress keys bindings by top-level attribute name — there is no support for sub-paths like <code>button.title</code>. Since <code>e-button</code> keeps all of its settings in a single <code>button</code> object attribute, an override stores <strong>the entire object</strong>, including the design props (<code>variant</code>, <code>size</code>, <code>icon</code>, <code>htmlElement</code>).</p><p>The practical consequence: once an instance has overridden a button, later design changes to that button in the pattern original no longer reach that instance. Its title and URL are its own, and so is its variant.</p><p>If a pattern&#39;s button styling needs to stay centrally controlled, keep overrides off for that button and let editors change copy in the pattern instead — or use separate patterns per variant. <code>e-accordion</code> and <code>e-tab-container</code> use flat string attributes and are not affected.</p><h2 id="extending-the-supported-attributes" tabindex="-1">Extending the supported attributes <a class="header-anchor" href="#extending-the-supported-attributes" aria-label="Permalink to &quot;Extending the supported attributes&quot;">​</a></h2><p>The list is a filter, so a project can expose more attributes on core blocks or opt its own blocks in:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add_filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webentor/block_bindings_supported_attributes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $map)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Let editors override the Section background image per instance.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    $map[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webentor/l-section&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;img&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Opt a theme block in.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    $map[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;webentor/my-testimonial&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;quote&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;author&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $map;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Only list content attributes, and prefer flat scalar attributes over objects for anything new — see the caveat above.</p><p>Because Pattern Overrides and Block Bindings share this list, every attribute here also becomes connectable to other sources such as post meta via the block&#39;s <strong>Attributes</strong> panel. That is usually a bonus, but it is a reason not to expose object attributes that no other source could sensibly produce.</p><h2 id="notes-for-block-authors" tabindex="-1">Notes for block authors <a class="header-anchor" href="#notes-for-block-authors" aria-label="Permalink to &quot;Notes for block authors&quot;">​</a></h2><p>Webentor blocks are dynamic (<code>save: () =&gt; null</code> plus a Blade <code>view.blade.php</code>), which is the easy case for bindings: WordPress resolves bound values before calling the render callback, so <code>$attributes</code> in your Blade view already holds the override. No <code>WP_HTML_Tag_Processor</code> work and no selectors are needed.</p><p>Do not add container blocks to the map. A block whose content is inner blocks has no attribute worth binding — its children are overridden individually instead, and adding it would only put a misleading entry in the <strong>Attributes</strong> panel. See <a href="#container-blocks-overriding-inner-content">Container blocks</a>.</p>`,35)])])}const u=t(i,[["render",o]]);export{k as __pageData,u as default};
